function change(amount) {
    var array = [1, 2, 5, 10, 50, 500, 1000];
    for (i in array) {
        var z = amount / array[i];
        if (z < 2) {
            x = array[i]
            break;
        }
        // console.log(array[i])
    }
    console.log(`${i} notes of ${array[i]}`)

    return "";

}
function change(amount, array) {
    while (amount > 0) {
        
        for()

        
    }
}
module.exports = { change }